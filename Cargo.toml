[package]
name = "slide"
version = "0.1.0"
edition = "2024"

[dependencies]


[features]
# default = ["nightly"]

# Enabling this feature tells the crate to optimize for file size.
opt_size = []

# Enabling this feature allows the crate to support features requiring nightly.
# nightly = []
